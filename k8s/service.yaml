apiVersion: v1
kind: Service
metadata:
  name: log-analyzer-service
spec:
  type: NodePort
  selector:
    app: log-analyzer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30007
